import { View, Text } from "react-native";
import useAuth from "../hooks/useAuth";

const LoginScreen = () => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
};

export default LoginScreen;
